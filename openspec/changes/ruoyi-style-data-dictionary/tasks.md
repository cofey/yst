## 1. 数据库与初始化

- [x] 1.1 在 `init.sql` 新增 `sys_dict_type`、`sys_dict_data` 表结构、索引与约束
- [x] 1.2 初始化“字典管理”菜单与 `system:dict:list|add|edit|remove` 权限点
- [x] 1.3 初始化首批基础字典类型与字典数据样例

## 2. 后端字典管理能力

- [x] 2.1 新增字典类型/字典数据实体、Mapper、VO 与请求模型
- [x] 2.2 实现字典类型与字典数据 CRUD 服务与校验规则（唯一性、状态、排序）
- [x] 2.3 新增字典管理 Controller，并接入 `@PreAuthorize("@ss.hasPermi('system:dict:*')")`
- [x] 2.4 实现按 `dictType` 查询启用字典项的消费接口
- [x] 2.5 实现字典缓存与失效逻辑（字典变更后精准清理）

## 3. 前端字典能力与页面

- [x] 3.1 新增字典相关 API 与 TypeScript 类型定义
- [x] 3.2 新增字典类型管理页面与字典数据管理页面（联动展示）
- [x] 3.3 新增统一字典加载/映射封装，供表单选项与值标签复用
- [x] 3.4 在菜单路由中注册“字典管理”并接入 `v-hasPermi` 按钮权限控制

## 4. 联调与验收

- [x] 4.1 覆盖字典管理与消费接口的后端测试（权限、唯一性、缓存失效）
- [x] 4.2 覆盖前端字典页面关键交互测试（类型-数据联动、权限显隐）