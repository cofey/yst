## 1. 键治理基线定义

- [x] 1.1 梳理全部主表并确定业务主键命名（`user_id/role_id/menu_id/company_id/...`）。
- [x] 1.2 梳理全部关联表并确定业务键外键列命名与类型（统一 `CHAR(36)`）。
- [x] 1.3 输出“禁止定义 `id` 自增主键”的数据库评审规则。

## 2. 数据库结构改造设计

- [x] 2.1 为主表定义统一 DDL 模板：`xxx_id` 主键 + `create_time/update_time`。
- [x] 2.2 为关联表定义统一 DDL 模板：业务键外键 + 联合唯一约束。
- [x] 2.3 定义外键约束策略（默认 `RESTRICT`）与异常处理规则。
- [x] 2.4 定义无软删除后的唯一约束与删除前校验策略。

## 3. 后端契约改造设计

- [x] 3.1 定义 DTO/VO 统一只暴露业务键的接口规范。
- [x] 3.2 定义 Service 层统一按业务键查询与关联的实现规则。
- [x] 3.3 定义鉴权与登录态字段统一使用业务键的切换规范。

## 4. 前端契约改造设计

- [x] 4.1 定义前端实体类型主标识为业务键字符串。
- [x] 4.2 定义路由参数、表格主键、操作动作统一使用业务键。
- [x] 4.3 定义接口适配层禁止透传内部数值型主键字段。

## 5. 迁移与验收

- [x] 5.1 定义迁移前数据巡检清单（业务键空值、重复值、孤儿关联）。
- [x] 5.2 定义分阶段发布方案（Schema -> Contract -> Governance）。
- [x] 5.3 定义回归验收矩阵（用户、角色、菜单、公司全链路 CRUD 与授权流）。
