## 1. 后端分页接口改造

- [x] 1.1 定义统一分页响应 VO（records/total/pageNum/pageSize）。
- [x] 1.2 用户列表接口支持分页与查询条件。
- [x] 1.3 角色列表接口支持分页与关键词/状态筛选。
- [x] 1.4 字典类型列表接口支持分页与关键词/状态筛选。
- [x] 1.5 字典数据列表接口支持分页与类型/标签/状态筛选。

## 2. 前端统一列表骨架

- [x] 2.1 用户页重构为统一布局（查询区 + 左工具栏 + 右查询操作 + 表格 + 分页）。
- [x] 2.2 角色页重构为统一布局并接入分页。
- [x] 2.3 字典页（类型/数据）重构为统一布局并接入分页。
- [x] 2.4 四类页面统一支持“查询/重置/展开收起”交互。

## 3. 交互规则校验

- [x] 3.1 确保页面初始化不自动查数据。
- [x] 3.2 确保仅点击查询触发请求。
- [x] 3.3 确保新增/导入/导出按钮位于表格左上角并按权限显示。

## 4. 验证

- [x] 4.1 前端 `pnpm run lint`。
- [x] 4.2 后端 `mvn -DskipTests compile`。
- [ ] 4.3 四类页面手工联调分页与查询流程。
