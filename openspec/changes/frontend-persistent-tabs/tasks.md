## 1. 规范与类型定义

- [x] 1.1 定义路由 `meta` 约定（`title`、`affix`、`cache`、`tab`）。
- [x] 1.2 定义 `TabItem` 结构与 `tabKey` 生成规则（含动态参数签名）。

## 2. 状态管理与持久化

- [x] 2.1 新增 tabs store，提供打开/关闭/激活/关闭其他/恢复/清空动作。
- [x] 2.2 接入 `localStorage` 持久化 `visitedTabs`、`activeTabKey`、`cachedViewKeys`。
- [x] 2.3 登录初始化恢复 tabs 状态；登出时清理 tabs 持久化数据。
- [x] 2.4 增加最大标签数控制（默认 20，超限淘汰最旧非固定标签）。

## 3. 布局与路由接入

- [x] 3.1 在主布局新增标签栏并接入切换、关闭、关闭其他交互。
- [x] 3.2 主内容渲染改造为 `router-view` 插槽 + `KeepAlive include`。
- [x] 3.3 为系统路由补齐 `meta` 字段，动态页接入参数化标签标题策略。
- [x] 3.4 恢复失败（路由不存在/无权限）回退到 `/home`。

## 4. 测试与验收

- [x] 4.1 增加 tabs store 单测（打开、关闭、恢复、上限淘汰）。
- [x] 4.2 增加动态路由标签键规则单测（同路由不同参数独立）。
- [x] 4.3 通过前端校验：`pnpm run lint`、`pnpm run test`。
- [ ] 4.4 手工回归：刷新恢复、动态多标签、登出清理、缓存开关行为。
