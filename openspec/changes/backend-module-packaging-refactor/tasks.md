## 1. OpenSpec 产物

- [x] 1.1 完成 proposal（目标、范围、影响）。
- [x] 1.2 完成 design（包结构、分页排序兼容设计）。
- [x] 1.3 完成 specs（模块化分包 + 分页排序兼容）。

## 2. 后端结构改造

- [x] 2.1 根包从 `com.yst` 迁移到 `com.shunbo.yst`。
- [x] 2.2 system 域按模块分包并在模块内分层（user/role/company/dict/menu）。
- [x] 2.3 `@MapperScan` 调整为模块 mapper 路径扫描。

## 3. 分页排序能力

- [x] 3.1 抽取分页公共查询对象（pageNum/pageSize + 排序兼容字段）。
- [x] 3.2 用户/角色/单位/字典类型/字典数据分页查询接入公共对象。
- [x] 3.3 各列表实现排序白名单并默认 `create_time desc`。

## 4. 前端兼容

- [x] 4.1 前端分页查询类型补充分页排序参数定义。
- [x] 4.2 列表 API 默认透传 `sortField=create_time`、`sortOrder=desc`。

## 5. 验证

- [x] 5.1 后端 `mvn -DskipTests compile` 通过。
- [x] 5.2 前端 `pnpm -C yst-ui type-check` 通过。
- [ ] 5.3 关键列表接口分页排序参数手工回归通过。

## 6. 菜单权限树渲染

- [x] 6.1 后端新增当前用户菜单树查询能力（按用户角色授权聚合菜单）。
- [x] 6.2 新增当前用户菜单树接口（推荐 `/api/auth/menus`）。
- [x] 6.3 前端 auth store 增加菜单树状态与持久化字段。
- [x] 6.4 登录流程接入菜单树拉取并写入 store。
- [x] 6.5 顶栏菜单改造为基于权限树全量动态渲染（替换写死菜单）。
- [x] 6.6 无权限路由访问改为“仅提示，不强制拦截”。
- [ ] 6.7 菜单权限树渲染与路由行为手工联调回归通过。
