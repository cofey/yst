## 1. 数据库与主数据模型

- [ ] 1.1 设计并落库 `sys_dept`、`sys_post`、`sys_role`、`sys_menu` 及 4 张关联表结构（含索引与唯一约束）。
- [ ] 1.2 补充 `sys_user` 组织与治理字段（`dept_id`、`del_flag` 等）并提供迁移脚本。
- [ ] 1.3 初始化基础字典数据（管理员角色、系统管理菜单、示例按钮权限点）。

## 2. 后端 RBAC 鉴权

- [ ] 2.1 将 JWT 主体切换为 `userId` 维度，并加入 `tokenVersion` 校验策略。
- [ ] 2.2 实现登录后权限装载服务（用户角色、菜单权限、按钮权限聚合）。
- [ ] 2.3 实现 `hasPermi`/`hasRole` 表达式组件并接入 Controller 注解鉴权。
- [ ] 2.4 建立角色/菜单/用户授权变更后的缓存失效机制。

## 3. 数据权限（DataScope）

- [ ] 3.1 定义 `data_scope` 枚举与角色配置规则（1/2/3/4/5）。
- [ ] 3.2 实现 `@DataScope` 注解与 AOP 条件注入能力。
- [ ] 3.3 在用户管理等首批查询接口接入数据权限并补全越权测试用例。

## 4. 前端权限联动

- [ ] 4.1 新增“当前用户路由树 + 按钮权限”接口调用与本地状态管理。
- [ ] 4.2 实现动态路由注册与刷新恢复策略。
- [ ] 4.3 实现 `v-hasPermi` 指令并在用户/角色/菜单页面落地。

## 5. 验收与治理

- [ ] 5.1 构建“角色 x 数据范围 x 菜单按钮”验收矩阵并执行联调。
- [ ] 5.2 完成日志扩展点（登录、授权变更、拒绝访问）并验证可追溯性。
- [ ] 5.3 通过后端 `mvn clean verify` 与前端 `npm run check` 门禁。
